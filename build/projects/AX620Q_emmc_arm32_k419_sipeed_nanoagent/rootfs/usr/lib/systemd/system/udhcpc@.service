[Unit]
Description=Run udhcpc service for %I
DefaultDependencies=no
After=boot.mount wifi-modules.service
Requires=boot.mount wifi-modules.service

[Service]
Type=simple
ExecStart=/sbin/udhcpc -i %I -s /usr/share/udhcpc/default.script -p /run/udhcpc.%I.pid
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
